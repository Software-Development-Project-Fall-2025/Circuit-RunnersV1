# Copy this file to .env and adjust values as needed
# Note: Many hosts will inject PORT automatically. For local dev you can leave PORT unset.

# Server port (default 3000 if unset)
# PORT=3000

# Environment: development or production
NODE_ENV=development

# In production, set this to your public site origin to tighten CORS
# Example: https://games.kent.edu/circuit-runners
CORS_ORIGIN=*
